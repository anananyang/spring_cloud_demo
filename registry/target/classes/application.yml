server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    # 不去服务端检索其他服务信息，因为自己就是服务端。
    fetch-registry: false
    # 因为自己就是服务端，所以不需要想服务端注册自己
    register-with-eureka: false
    # 暴露给外部的注册中心位置
    service-url:
      defaultZone: http://localhost:8761/eureka
